@model IEnumerable<LeftAside>

<aside class="left">
    @foreach (LeftAside item in Model)
    {
        <div>
            <a href="@item.Link" class="@item.ColorClass" target="_blank">@item.Title</a>
            <a href="@item.Link" class="@item.ColorClass" target="_blank"> <i class="@item.IconClass"></i> </a>
        </div>
    }
</aside>